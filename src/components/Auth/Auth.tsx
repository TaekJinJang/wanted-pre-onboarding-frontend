import * as S from '../../styles/Auth.styled';

const Auth = () => {
    return (
        <S.AuthContainer>
            <S.AuthTitle>
                <h1>회원가입</h1>
            </S.AuthTitle>
        </S.AuthContainer>
    );
};

export default Auth;
